ProcessResponse() {
  Log debug "Got args $*"
  local response_body="$1"
  local request_keys=($2)

  unset key_values
  declare -Ag key_values

  for key in "${request_keys[@]}"; do
    Log debug "Trying key '$key'"
    key_values+=(["$key"]="$(jq -r -e .\"$key\" <<< $response_body)")
    Log debug "Got value '${key_values[$key]}'"
  done
}
